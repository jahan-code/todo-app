const { errorCode } = require('./error.constant');
const { randomConstant } = require('./random.constant');
const { responseCode } = require('./response.constant');

module.exports = {
  responseCode,
  errorCode,
  randomConstant,
};
