const signUp = require('./signUp.content');
const deals = require('./deals.content');
const agreement = require('./agreements.content');
const subscription = require('./subscription.content');
const eviction = require('./eviction.content');

module.exports = {
  signUp,
  deals,
  agreement,
  subscription,
  eviction,
};
